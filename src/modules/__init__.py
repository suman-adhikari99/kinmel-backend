# Domain Modules
# Each module is a self-contained bounded context with its own:
# - Domain models (entities, value objects)
# - Repository (data access)
# - Service (business logic)
# - Router (API endpoints)
# - Schemas (request/response DTOs)

